import AuthenticationForm from "../modules/AuthenticationForm";

const SignInPage = () => {
  return (
    <section>
      <div>
        <AuthenticationForm />
      </div>
    </section>
  );
};

export default SignInPage;
